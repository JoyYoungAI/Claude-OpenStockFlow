# Codex 版專案工作規範

這份文件用來約定「哪些資訊放在哪裡、什麼時候用什麼工具、怎麼讓 Codex 穩定協作」。它不是 Claude Code 的設定檔，而是適合 Codex workspace 的專案工作方式。

## 核心原則

- 專案長期規則放在文件裡，避免每次對話重講。
- 可重複的專門流程做成 Skill，避免把所有細節塞進單一文件。
- 能自動檢查的事情交給測試、腳本或自動化，不靠人記。
- 外部資料、服務、工具用 MCP 或專用工具接入，不硬寫在提示詞裡。
- 大任務先拆清楚，再讓 Codex 分段探索、修改、驗證。

## 組件對照

| 組件 | 在 Codex 裡是什麼 | 什麼時候使用 | 適合放什麼 | 常見誤區 |
|---|---|---|---|---|
| 專案說明文件 | `README.md`、`CONTRIBUTING.md`、`CODEX_PROJECT_GUIDE.md` 等 | 每次協作前都可讀取 | 專案目標、架構、開發流程、命名規則、測試方式 | 把大量任務細節和一次性指令全塞進 README |
| Skills | Codex 的任務型指令包 | 任務需要專門流程時 | 文件、簡報、試算表、圖片、瀏覽器測試、團隊自訂工作流 | 把應該可重用的專業流程只寫在聊天紀錄裡 |
| Plugins | 一組 skills、工具、MCP 或 app 能力 | 需要打包分發能力時 | Browser、Documents、Presentations、Spreadsheets，或團隊內部工具包 | 每個專案都手動重配同一套工具 |
| 自動化 | Codex app 的 recurring automation / heartbeat | 需要定期檢查、提醒、追蹤時 | 例行回顧、定期報告、監控任務、稍後續做 | 把「明天提醒我」留在腦中或聊天中 |
| MCP / 工具 | 連接外部系統和本機能力的接口 | 需要讀資料、操作服務、跑工具時 | API、資料庫、瀏覽器、本機資源、文件系統 | 基礎流程沒穩前就急著接很多外部工具 |
| LSP / 型別檢查 | 語言層級的程式碼理解與檢查 | 程式碼專案中持續使用 | 型別錯誤、引用查找、語法診斷、重構輔助 | 以為 Codex 會自動知道所有語言伺服器設定 |
| 多代理 / 子任務 | 用獨立工作流拆分探索、分析、修改 | 大型任務或可平行工作時 | 程式碼審查、研究、測試、遷移、規格拆解 | 把探索、設計、修改、驗證全部混在一次操作裡 |

## 建議檔案結構

```text
project/
  README.md
  CONTRIBUTING.md
  CODEX_PROJECT_GUIDE.md
  docs/
    architecture.md
    decisions/
  scripts/
    check.*
    test.*
  .codex/
    notes/
```

## 文件分工

`README.md`

放專案對外或對新成員最重要的資訊：專案是什麼、怎麼啟動、怎麼測試、主要目錄用途。

`CONTRIBUTING.md`

放協作規則：分支策略、提交規範、PR 檢查、程式風格、測試要求。

`CODEX_PROJECT_GUIDE.md`

放給 Codex 協作時要遵守的工作方式：如何探索、如何修改、如何驗證、哪些事情需要先問使用者。

`docs/architecture.md`

放系統架構、資料流、關鍵模組、外部依賴、部署方式。

`docs/decisions/`

放重要決策紀錄。每個決策一篇，包含背景、選項、取捨、決定、後續影響。

## 給 Codex 的協作規則

1. 先讀專案文件，再動手改程式。
2. 優先使用既有框架、工具、命名和風格。
3. 修改範圍要貼近任務，不做無關重構。
4. 改動前先理解測試方式；改動後盡量跑相關檢查。
5. 如果工作區已有使用者改動，不要還原，必須與現況共存。
6. 需要網路、外部服務、破壞性操作或敏感資料時，先取得明確許可。
7. 完成後回報改了什麼、驗證了什麼、還有什麼風險。

## 任務分類與做法

### 小型修改

適合範圍：文案、單一 bug、小型 UI 調整、單一函式修改。

建議流程：

1. 找到相關檔案。
2. 閱讀上下文。
3. 小範圍修改。
4. 跑最相關的檢查。
5. 簡短回報。

### 中型功能

適合範圍：新增頁面、API、資料流程、跨兩三個模組的改動。

建議流程：

1. 梳理現有架構和相似實作。
2. 列出實作步驟。
3. 先做核心路徑。
4. 補錯誤狀態、空狀態和測試。
5. 用實際操作或測試驗證。

### 大型任務

適合範圍：重構、遷移、多模組功能、效能改善、設計系統調整。

建議流程：

1. 先盤點影響範圍。
2. 拆成探索、方案、實作、驗證幾段。
3. 每段完成後同步狀態。
4. 保留可回退的中間結果。
5. 優先交付可驗證的小成果。

## 什麼該做成 Skill

適合做成 Skill 的內容：

- 多個專案都會重複使用的流程。
- 需要固定檢查清單的任務。
- 有專門輸出格式的工作。
- 需要引用範本、腳本或資產的流程。

不適合做成 Skill 的內容：

- 只對單一專案有效的臨時規則。
- 一次性任務。
- 還沒穩定下來的實驗做法。

## 什麼該做自動化

適合自動化：

- 每天/每週要檢查的狀態。
- 需要定期提醒的事項。
- 需要稍後繼續的長任務。
- 需要監控變化並回報的工作。

不適合自動化：

- 需要大量主觀判斷的事情。
- 權限、成本或風險很高的操作。
- 還沒定義清楚成功條件的任務。

## 驗證標準

完成任務前，至少確認其中一項：

- 相關測試通過。
- 型別檢查或 lint 通過。
- 本機頁面或功能已實際操作過。
- 文件輸出已渲染或打開檢查。
- 無法驗證時，明確說明原因和剩餘風險。

## 回報格式

建議 Codex 完成後用這種格式回報：

```text
已完成：簡述完成內容。
改動位置：列出主要檔案。
驗證：說明跑過的檢查或實測結果。
注意：剩餘風險、沒跑的測試、需要你決定的事。
```

## 最小可用版本

如果專案剛開始，只要先保留三件事：

1. `README.md`：專案怎麼跑。
2. `CODEX_PROJECT_GUIDE.md`：Codex 怎麼協作。
3. `scripts/check.*` 或清楚的檢查指令：怎麼確認沒有壞。

