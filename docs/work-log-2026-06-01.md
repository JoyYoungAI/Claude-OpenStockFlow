# StockFlow work log - 2026-06-01

## Audit trail first implementation

Completed:
- Added the audit trail baseline spec and linked it from the inventory system specification.
- Added first-stage audit trail storage as `auditLogs`.
- Raised localStorage `schemaVersion` to `10`.
- Kept audit logs in backup, restore, and sample-data reset flows.
- Added audit query UI under reports.
- Added audit CSV export.
- Added report print tracking through the in-app print button.
- Added audit events for core create, update, void/delete, return, reversal, export, restore, role access, permission denied, and include-voided sensitive reads.

## Pre-change backup

Created:
- `backups/pre-change/stockflow-prechange-20260601-191550-audit-trail-implementation.zip`

## Verification

Passed:
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryModels.js`
- `node --check apps\inventory-system\inventoryStore.js`
- `node --check apps\inventory-system\inventoryReports.js`
- `node --check apps\inventory-system\inventoryStorage.js`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

Browser note:
- In-app Browser verification remains unavailable in this environment, so this pass used syntax checks, store tests, static UI markers, backup/schema checks, and the full project check script.

## Organization permission v2 planning

Checked import status:
- Current system supports full JSON backup restore as whole-package import.
- JSON restore carries `auditLogs`, document lifecycle fields, returns, cost layers, receivables, payables, payments, and relationships.
- There is no CSV import for departments, employees, permission scopes, or transaction documents yet.

Added:
- `docs/inventory/organization-permission-v2-baseline.md`
- Main inventory spec link for the organization/personnel permission v2 baseline.
- `scripts/check.ps1` guard markers for the new baseline.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260601-193353-organization-permission-v2-spec.zip`

## Organization permission v2 first implementation

Completed:
- Added department, employee, and permission-scope master data to the local store and backup schema.
- Raised localStorage `schemaVersion` to `11`.
- Added first-stage department and employee maintenance UI under basic data.
- Added a local current-employee selector in the header.
- Kept the legacy local role selector as fallback, but locked it when a current employee is selected.
- Upgraded `canPerform(action, context)` so it can evaluate role, employee, department, owner, and supervisor scope.
- Added audit actor fields for employee and department context.
- Added backup summary counts for departments, employees, and permission scopes.
- Updated project checks to require the new master data, selector, permission-scope, and schema markers.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260601-193651-organization-permission-v2-implementation.zip`

Verification:
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryModels.js`
- `node --check apps\inventory-system\inventoryStore.js`
- `node --check apps\inventory-system\inventoryReports.js`
- `node --check apps\inventory-system\inventoryStorage.js`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

Notes:
- This is the first implementation slice: master data, current employee context, and permission-scope groundwork are in place.
- Transaction ownership fields and document-row action guards can be tightened in the next slice.

## Access layer refactor

Completed:
- Added `docs/inventory/access-layer-refactor-baseline.md`.
- Linked the access layer refactor baseline from the main inventory system spec.
- Added `apps/inventory-system/inventoryAccess.js`.
- Moved role definitions, operation permissions, module permissions, field permissions, supervisor scope checks, and permission wording into the access layer.
- Kept `app.js` wrapper functions such as `canPerform`, `canViewModule`, and `permissionReason` so existing UI code remains stable.
- Updated `index.html` script loading and `scripts/check.ps1` guards for the new access module.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260601-201801-access-layer-refactor.zip`

Verification:
- `node --check apps\inventory-system\inventoryAccess.js`
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryModels.js`
- `node --check apps\inventory-system\inventoryStore.js`
- `node --check apps\inventory-system\inventoryReports.js`
- `node --check apps\inventory-system\inventoryStorage.js`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

Line count note:
- `app.js`: 3,069 lines after the refactor.
- `inventoryAccess.js`: 183 lines.
- `inventoryStore.js`: 1,947 lines.

Follow-up fix:
- Fixed a runtime tab-switching failure caused by one leftover direct `accessRoles` reference in `app.js` after moving the access policy into `inventoryAccess.js`.
- Updated `scripts/check.ps1` to fail if `app.js` directly references access-layer policy globals after the refactor.
- Re-ran syntax checks, store tests, and the full project check. Result: `All checks passed.`

## Audit layer refactor

Completed:
- Added `docs/inventory/audit-layer-refactor-baseline.md`.
- Linked the audit layer refactor baseline from the main inventory system spec.
- Added `apps/inventory-system/inventoryAudit.js`.
- Moved audit filter option collection, sensitive read payload creation, audit CSV formatting, audit labels, and audit badges into the audit layer.
- Kept `recordAudit` in `app.js` as the current-user and persistence boundary.
- Updated `index.html` script loading and `scripts/check.ps1` guards for the new audit module.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260601-204334-audit-layer-refactor.zip`

Verification:
- `node --check apps\inventory-system\inventoryAudit.js`
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryAccess.js`
- `node --check apps\inventory-system\inventoryModels.js`
- `node --check apps\inventory-system\inventoryStore.js`
- `node --check apps\inventory-system\inventoryReports.js`
- `node --check apps\inventory-system\inventoryStorage.js`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

Line count note:
- `app.js`: 3,041 lines after the audit refactor.
- `inventoryAudit.js`: 98 lines.
- `inventoryAccess.js`: 183 lines.
- `inventoryStore.js`: 1,947 lines.

## Backup layer refactor

Completed:
- Added `docs/inventory/backup-layer-refactor-baseline.md`.
- Linked the backup layer refactor baseline from the main inventory system spec.
- Added `apps/inventory-system/inventoryBackup.js`.
- Moved backup filename creation, JSON backup download, backup file reading, backup validation callbacks, and backup summary rendering into the backup layer.
- Kept permission checks, pending restore state assignment, active store replacement, audit recording, and final persistence in `app.js`.
- Updated `index.html` script loading and `scripts/check.ps1` guards for the new backup module.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260601-204733-backup-layer-refactor.zip`

Verification:
- `node --check apps\inventory-system\inventoryBackup.js`
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryAudit.js`
- `node --check apps\inventory-system\inventoryAccess.js`
- `node --check apps\inventory-system\inventoryModels.js`
- `node --check apps\inventory-system\inventoryStore.js`
- `node --check apps\inventory-system\inventoryReports.js`
- `node --check apps\inventory-system\inventoryStorage.js`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

Line count note:
- `app.js`: 3,025 lines after the backup refactor.
- `inventoryBackup.js`: 63 lines.
- `inventoryAudit.js`: 98 lines.
- `inventoryAccess.js`: 183 lines.
