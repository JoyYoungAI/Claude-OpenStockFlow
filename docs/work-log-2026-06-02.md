# StockFlow work log - 2026-06-02

## Master data UI refactor

Completed:
- Added `docs/inventory/master-data-ui-refactor-baseline.md`.
- Linked the master data UI refactor baseline from the main inventory system spec.
- Added `apps/inventory-system/inventoryMasterDataUi.js`.
- Moved product category option rendering, department option rendering, partner datalist rendering, product category table rendering, warehouse table rendering, department table rendering, employee table rendering, and partner table rendering into the master data UI layer.
- Kept master data form submission, edit state, deactivate click handlers, permission checks, and audit recording in `app.js`.
- Updated `index.html` script loading and `scripts/check.ps1` guards for the new master data UI module.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260602-070539-master-data-ui-refactor.zip`

Verification:
- `node --check apps\inventory-system\inventoryMasterDataUi.js`
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryAccess.js`
- `node --check apps\inventory-system\inventoryAudit.js`
- `node --check apps\inventory-system\inventoryBackup.js`
- `node --check apps\inventory-system\inventoryModels.js`
- `node --check apps\inventory-system\inventoryStore.js`
- `node --check apps\inventory-system\inventoryReports.js`
- `node --check apps\inventory-system\inventoryStorage.js`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

## Morning lightweight handoff and planning

Completed:
- Added `docs/HANDOFF-SHORT-2026-06-02.md` for fast thread restoration.
- Added `docs/inventory/manual-ui-checklist-v1.17.1.md` for the next Browser or real-browser manual UI pass.
- Added `docs/inventory/version-candidates-after-v1.17.1.md` with `v1.17.2`, `v1.17.3`, and `v1.18.0` candidate paths.
- Kept this slice documentation-only; no app behavior, data model, or UI code was changed.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260602-085814-morning-light-handoff-planning.zip`

## v1.17.2 responsibility visibility polish

Completed:
- Raised app and asset versions to `1.17.2`.
- Updated `index.html` cache-busting query strings and `scripts/check.ps1` version guards.
- Added purchase/sale row responsibility text: `負責：人員 / 部門`.
- Added `未指派` fallback when owner employee or owner department is empty or unavailable.
- Added `documentResponsibilityText`, `employeeName`, and `departmentName` helpers in `app.js`.
- Added i18n keys for responsibility display text.
- Added `docs/inventory/manual-ui-checklist-v1.17.2.md` for the next Browser or real-browser manual UI pass.
- Updated handoff, short handoff, architecture, and main inventory spec notes.
- Kept this slice display-only: no lifecycle, stock, finance, return, void, or permission behavior changed.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260602-090734-version-1-17-2-responsibility-visibility.zip`

Line count note:
- `app.js`: 3,034 lines after the responsibility visibility polish.
- `inventoryMasterDataUi.js`: 179 lines.
- `inventoryAccess.js`: 202 lines.
- `inventoryAudit.js`: 98 lines.
- `inventoryBackup.js`: 63 lines.
- `inventoryStore.js`: 2,029 lines.

Verification:
- `node --check apps\inventory-system\app.js`
- `node --check apps\inventory-system\inventoryI18n.js`
- PowerShell AST parse for `scripts\check.ps1`
- `node apps\inventory-system\inventoryStore.test.js`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`

Result:
- `scripts/check.ps1` ended with `All checks passed.`

## Document ownership permission context slice

Completed:
- Updated `docs/inventory/purchase-spec.md` and `docs/inventory/sales-spec.md` with purchase/sale ownership fields.
- Added purchase and sale ownership preservation for `ownerEmployeeId`, `ownerDepartmentId`, `createdByEmployeeId`, and `lastEditedByEmployeeId`.
- Added ownership to current sample purchase and sale documents.
- Passed target document context into purchase/sale row actions for workflow, return, void, and void reversal checks.
- Added backup validation for purchase/sale owner employee and owner department references when those fields are present.
- Updated `apps/inventory-system/inventoryStore.test.js` and `scripts/check.ps1` guards for document ownership and permission context.
- Kept direct `voidDocument` authority unchanged in this slice; supervisor expansion still needs explicit lifecycle-safe edit/delete rules before opening wider permissions.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260602-074619-document-ownership-permission-context.zip`

Verification:
- Bundled Node syntax checks for inventory app, access, models, store, storage, audit, backup, master data UI, and reports modules.
- `node apps\inventory-system\inventoryStore.test.js` using bundled Node.
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1` with bundled Node first in PATH.

Result:
- `scripts/check.ps1` ended with `All checks passed.`

## v1.17.1 version and supervisor safety slice

Completed:
- Raised app and asset versions to `1.17.1`.
- Updated `index.html` cache-busting query strings and `scripts/check.ps1` version guards.
- Updated architecture, handoff, and main inventory spec notes for `v1.17.1`.
- Kept localStorage schemaVersion at `11`; the new ownership fields are backward-compatible and normalize to empty IDs when old data is loaded.
- Added scoped supervisor actions for returning unconfirmed purchase/sale documents.
- Added scoped supervisor action to reassign unconfirmed purchase/sale document responsibility with the `改由我負責` row action.
- Kept confirmed documents protected: no supervisor voiding, no confirmed-document direct edit, no stock or finance reversal expansion.
- Added store tests for supervisor-only scope checks and responsibility reassignment lifecycle limits.
- Updated `scripts/check.ps1` guards for `v1.17.1`, target document context, ownership fields, and supervisor-safe actions.

Pre-change backup:
- `backups/pre-change/stockflow-prechange-20260602-083431-version-1-17-1-supervisor-safe-actions.zip`

Verification:
- Bundled Node syntax checks for `app.js`, `inventoryAccess.js`, `inventoryStore.js`, and `inventoryI18n.js`.
- `node apps\inventory-system\inventoryStore.test.js` using bundled Node.
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1` with bundled Node first in PATH.

Result:
- `scripts/check.ps1` ended with `All checks passed.`
